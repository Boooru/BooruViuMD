#:import FadeTransition kivy.uix.screenmanager.FadeTransition
#:import SettingsProviderScreen ui.screens.settings_screens.SettingsProviderScreen
#:import RootScrollScreen ui.screens.home_screen.HomeScreen
#:import BigViewScreen ui.screens.big_view_screen.BigViewScreen
#:import SettingsScreen ui.screens.settings_home_screen.SettingsHomeScreen

MDNavigationLayout:
    id: root_layout

    ScreenManager:
        id: screen_manager
        transition: FadeTransition()

        HomeScreen:
            name: "home screen"

        BigViewScreen:
            name: "big view screen"

        SettingsHomeScreen:
            name: "settings home screen"

        SettingsAppearanceScreen:
            name: "settings appearance screen"

        SettingsProviderScreen:
            name: "provider setup screen"

    MDNavigationDrawer:
        id: nav_drawer
        ContentNavigationDrawer:
