#:import FadeTransition kivy.uix.screenmanager.FadeTransition
#:import ProviderSetupScreen ui.screens.ProviderSetupScreen
#:import RootScrollScreen ui.screens.RootScrollScreen
#:import BigViewScreen ui.screens.BigViewScreen

ScreenManager:
    transition: FadeTransition()

    ProviderSetupScreen:
        name: "provider setup screen"

    RootScrollScreen:
        name: "root scroll screen"

    BigViewScreen:
        name: "big view screen"