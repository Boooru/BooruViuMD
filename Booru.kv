#:import FadeTransition kivy.uix.screenmanager.FadeTransition
#:import ProviderSetupScreen ui.screens.screen_util.ProviderSetupScreen
#:import RootScrollScreen ui.screens.home_screen.HomeScreen
#:import BigViewScreen ui.screens.big_view_screen.BigViewScreen

MDNavigationLayout:
    id: root_layout

    ScreenManager:
        id: screen_manager
        transition: FadeTransition()

        HomeScreen:
            name: "home screen"

        ProviderSetupScreen:
            name: "provider setup screen"

        BigViewScreen:
            name: "big view screen"

    MDNavigationDrawer:
        id: nav_drawer
        ContentNavigationDrawer:
